{
  "name": "leetcodejsbootcamp",
	"version": "1.0.0",
	"description": "cd to working folder and run 'npm run jest'",
	"main": "index.js",
	"scripts": {
		"jest": "jest",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"jest": {
		"testEnvironment": "node"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
    "jest": "^26.6.3"
  }
}
